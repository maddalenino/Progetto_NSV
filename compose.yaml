services:
  web:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - redis
    environment:
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      PORT: "5000" 
  
  redis:
    image: redis:alpine
    volumes:
      - redis-data:/data

volumes:
     redis-data: